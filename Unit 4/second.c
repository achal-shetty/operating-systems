#include<stdio.h>
#include<sys/stat.h>
#include<fcpnl.h>
#include<stdlib.h>
#include<string.h>

int main()
{
	int fd;
	int i, err= 0;
	char buff[10] = "";
	fd = creat["~/Desktop/Operating Systems/29June", S_]

	if(fd == 1)
		printf("Unable to create a file\n");
	for(i=0; i<10; i++)
		write(fd, "Welcome to file system",22);
	close(fd);

	fd = open()

	err = lseek(fd, )
}
